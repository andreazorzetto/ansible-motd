## Customize the test machine
machine:

## Customize dependencies
dependencies:
  pre:
    - pip install ansible testinfra # install from a different package manager

## Customize test commands
test:
  override:
    - testinfra circleci_test.py # use PHPunit for testing
